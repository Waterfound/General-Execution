[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "general-execution"
version = "0.0.13"
description = "Provider-neutral, deterministic execution substrate for bounded authorized work"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}
authors = [{name = "Waterfound"}]
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8"]
# The v1 conformance protocol uses the synchronous Sandbox.create API.
# New SDK generations change both this API and the default VM image.
vercel-sandbox = ["vercel==0.5.9"]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
